Verification push after removing the duplicate `tennis10` Vercel project and
reconnecting the GitHub integration — confirming `tennisalpha` deploys again.

Second verification: Root Directory fixed to `trading-terminal` in Vercel
project settings (2026-09-23) — confirming the automatic GitHub deploy
trigger now works without a manual `vercel --prod` fallback.
